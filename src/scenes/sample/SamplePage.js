import React from 'react';
import SampleComponentData from '../../containers/SampleComponentData';

export default function SamplePage() {
    return (
        <section>
            <h4>On this page you can see a sample data from server</h4>
            <SampleComponentData />
        </section>
    );
}
