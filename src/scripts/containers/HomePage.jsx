import React from 'react';

export default function HomePage() {
    return (
        <section>
            <h4>Welcome to React Starter Kit</h4>
            <p>{'Click on "Sample Page" link to see page with sample data from server'}</p>
        </section>
    );
}
